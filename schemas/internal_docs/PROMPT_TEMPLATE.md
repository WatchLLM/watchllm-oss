⇒ WatchLLM Schemas — 4-Day Prompt Pack

Copy **one full day block** below into your agent session. Each block uses the same template; only task, gate, and scope placeholders are filled from `internal_docs/IMPORTANT.md`.

**Repo:** `watchllm/schemas` (local: `d:\watchllm\schemas`)

---

⇒ Day 01 — Task 01: Skeleton

⇒ Task Instructions

Today's task:
**SCHEMAS Task 01 — Skeleton**
Create the `schemas/v1/` directory structure.

Today's gate:
- Structure is created.

> Scope Constraints
Only work on: Directory creation (Task 01 only).

---

⇒ Day 02 — Task 02: Event Schema

⇒ Task Instructions

Today's task:
**SCHEMAS Task 02 — Event Schema**
Draft `schemas/v1/event.json` containing the trace logging definitions (`parse_started`, `telemetry_relayed`).

Today's gate:
- `event.json` is a valid Draft-07 schema.

> Scope Constraints
Only work on: `event.json` (Task 02 only).

---

⇒ Day 03 — Task 03: Violation Schema

⇒ Task Instructions

Today's task:
**SCHEMAS Task 03 — Violation Schema**
Draft `schemas/v1/violation.json` standardizing code boundary and credential leak payloads.

Today's gate:
- `violation.json` is a valid Draft-07 schema.

> Scope Constraints
Only work on: `violation.json` (Task 03 only).

---

⇒ Day 04 — Task 04: Decision Schema

⇒ Task Instructions

Today's task:
**SCHEMAS Task 04 — Decision Schema**
Draft `schemas/v1/decision.json` defining the ALLOW/BLOCK/INCONCLUSIVE outcome payload.

Today's gate:
- `decision.json` is a valid Draft-07 schema.

> Scope Constraints
Only work on: `decision.json` (Task 04 only).
